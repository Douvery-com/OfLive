export enum LayoutOptions {
  Contain = 'constrained',
  FullWidth = 'fullWidth',
  Fixed = 'fixed',
}

//
export enum LayoutOrienntationsOptions {
  Horizontal = 'horizontal',
  Vertical = 'vertical',
}
