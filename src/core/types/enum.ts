export enum LayoutOptions {
  Contain = 'constrained',
  FullWidth = 'fullWidth',
  Fixed = 'fixed',
}

export enum ObjectFitOptions {
  Contain = 'contain',
  Cover = 'cover',
  Fill = 'fill',
  None = 'none',
  ScaleDown = 'scale-down',
  Inherit = 'inherit',
  Initial = 'initial',
}

export enum LayoutOptions {
  Horizontal = 'horizontal',
  Vertical = 'vertical',
}
